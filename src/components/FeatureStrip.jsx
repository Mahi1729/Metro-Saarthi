import { BadgeIndianRupee, Clock3, GitBranch, Route, Waypoints } from 'lucide-react'
export function FeatureStrip() { const features = [[Route, 'Shortest route', 'Dijkstra finds the leanest path.'], [Waypoints, 'Fewer stops', 'BFS keeps the journey moving.'], [GitBranch, 'Smart changes', 'Interchanges are marked for you.'], [BadgeIndianRupee, 'Fare estimate', 'Simple slabs, clear costs.'], [Clock3, 'Time aware', 'Travel and dwell time combined.']]; return <section className="feature-strip" id="how-it-works"><div className="feature-intro"><span className="section-kicker">Built on structure</span><h2>Every journey,<br /><i>thought through.</i></h2></div>{features.map(([Icon, title, copy]) => <div className="feature-item" key={title}><Icon size={19} /><strong>{title}</strong><p>{copy}</p></div>)}</section> }
