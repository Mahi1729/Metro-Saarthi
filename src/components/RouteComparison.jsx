import { ArrowRight, Clock3, MapPin, Route as RouteIcon } from 'lucide-react'
export function RouteComparison({ routes, active, onSelect }) { if (!routes) return null; const options = [{ key: 'distance', label: 'Shortest distance', icon: RouteIcon }, { key: 'stops', label: 'Minimum stops', icon: MapPin }]; return <div className="comparison"><div className="comparison-head"><div><span className="section-kicker">Compare paths</span><h3>Two ways through the city</h3></div><Clock3 size={18} /></div><div className="comparison-options">{options.map(({ key, label, icon: Icon }) => { const route = routes[key]; return <button key={key} type="button" className={`comparison-option ${active === key ? 'selected' : ''}`} onClick={() => onSelect(route, key)}><div className="compare-title"><span className="compare-icon"><Icon size={16} /></span><strong>{label}</strong>{active === key && <span className="chosen">Selected</span>}</div><div className="compare-values"><span>{route.distance.toFixed(1)} km</span><span>{route.stops} stops</span><span>{route.time} min</span><ArrowRight size={15} /></div></button> })}</div></div> }
